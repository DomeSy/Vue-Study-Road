什么是vue的计算属性
1>在模板中放入太多的逻辑会让模板过重且难以维护，在需要对数据进行复杂处理，且可能多次使用的情况下，尽量采取计算属性的方式。
2>好处：
a.使得数据处理结构清晰；
b.依赖于数据，数据更新，处理结果自动更新；
c.计算属性内部this指向vm实例；
d.在template调用时，直接写计算属性名即可；
e.常用的是getter方法，获取数据，也可以使用set方法改变数据；
f.相较于methods，不管依赖的数据变不变，methods都会重新计算，但是依赖数据不变的时候computed从缓存中获取，不会重新计算。
